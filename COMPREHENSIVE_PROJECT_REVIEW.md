
# تقرير مراجعة شاملة لمشروع خالة العيال

## 1. استكشاف بنية المشروع الحالية
تم استكشاف جميع ملفات المشروع، ووجود التالي:
- **الواجهات الأمامية**: React + TypeScript + Taro
- **الواجهات الخلفية**: إستخدام Supabase كـ Backend-as-a-Service (BaaS)
- **الخدمات**: موجودة في `src/services/`:
  - `booking.ts`: إدارة الحجوزات
  - `children.ts`: إدارة الأطفال
  - `chat.ts`: خدمة المحادثات
  - `location.ts`: تتبع المواقع
  - `notification.ts`: إدارة الإشعارات
  - `review.ts`: إدارة التقييمات
  - `wallet.ts`: إدارة المحافظ والسحوبات
  - `sitter.ts`: إدارة ملفات الجليسات
- **النوعين**: موجودة في `src/types/`:
  - `core.ts`: أنواع أساسية للمشروع
  - `dispute.ts`: أنواع لنظام النزاعات والدعم الفني (موجودة لكن لم يتم تنفيذها)

---

## 2. جداول قاعدة البيانات والعلاقات (تم التحديث)

### 2.1 الجداول المضافة الآن إلى `schema.sql` ✅
- **جدول النزاعات (`disputes`)** – تم إضافته مع جميع الأعمدة والسياسات الأمنية
- **جدول تذاكر الدعم الفني (`support_tickets`)** – تم إضافته مع جميع الأعمدة والسياسات الأمنية
- **جدول رسائل التذاكر (`ticket_messages`)** – تم إضافته مع جميع الأعمدة والسياسات الأمنية
- **جدول الدفعات (`payments`)** – تم إضافته مع جميع الأعمدة والسياسات الأمنية

### 2.2 الجداول المضافة مؤخرًا إلى `schema.sql`
- تم إضافة جدول `sitter_locations`
- تم إضافة الأعمدة `mother_job`, `father_job`, `default_address` إلى جدول `profiles`
- تم إضافة `DROP FUNCTION IF EXISTS ... CASCADE` قبل كل دالة لمنع الأخطاء
- تم إضافة `DROP POLICY IF EXISTS` قبل كل سياسة RLS
- تم إضافة `ALTER TABLE profiles ADD COLUMN IF NOT EXISTS` للعامود الجديد

---

## 3. الميزات الواجهة الخلفية غير المنفذة
### 3.1 نظام النزاعات والدعم الفني
- أنواع الميزة موجودة في `dispute.ts` لكن غير موجودة في:
  - `schema.sql`: لا توجد جداول للنزاعات والتذاكر
  - الخدمات: لا توجد خدمات لإنشاء/عرض/تحديث النزاعات والتذاكر
  - الواجهات الأمامية: لا توجد واجهات للنزاعات أو الدعم الفني

### 3.2 نظام الدفع الكامل
- الواجهة `PaymentPage.tsx` موجودة لكنها فقط **محاكاة**
- لا توجد تكامل فعلي مع بوابة دفع (مثل Fawry/PayMob/Stripe)
- ✅ **تم إضافة جدول `payments` في قاعدة البيانات لتسجيل جميع المعاملات**
- لا توجد منطقية للتحقق من إتمام الدفع وتحديث حالة الحجز تلقائيًا

### 3.3 إدارة الإشعارات المتقدمة
- لا توجد منطقية لإرسال إشعارات فعلية (لا يوجد تكامل مع Push Notifications/Email/SMS)
- لا توجد وظيفة لجدولة الإشعارات المستقبلية

---

## 4. تحليل مشكلة تعطل قاعدة البيانات مع عدد كبير من المستخدمين

### 4.1 أسباب تعطل قاعدة البيانات في الخطة المجانية من Supabase
بموجب بحثنا عن وثائق Supabase الرسمية، الخطة المجانية لديها:
- **حد الاتصالات المباشرة**: 60 اتصال مباشر فقط
- **موارد حوسبة مشتركة**: وحدة معالجة مركزية وذاكرة مشتركة مع مستخدمين آخرين
- **إيقاف المشروع تلقائي**: بعد أسبوع من عدم النشاط
- **حد تخزين**: 500MB فقط لقاعدة البيانات

عند وجود عدد كبير من طلبات التسجيل أو الاستخدام في نفس الوقت:
1. يتجاوز عدد الاتصال المحدد
2. تُرفض الاتصالات الجديدة بواسطة Postgres بالخطأ: `FATAL: sorry, too many clients already`
3. تتعطل الخدمة الكاملة

### 4.2 التوصية بخطة Supabase المناسبة
- **للإطلاق المبدئي مع عدد محدود من المستخدمين**: خطة **Pro** (من $25/شهر)
  - توفير موارد حوسبة مخصصة (Micro: 1GB RAM, 2-core CPU)
  - 200 اتصال مباشر أو 800 اتصال عبر PgBouncer
  - 8GB مساحة تخزين لقاعدة البيانات
  - 100,000 مستخدم نشط شهريًا
  - نسخ احتياطي يومية
- **للنمو والارتقاء**: ترقية موارد الحوسبة إلى Small/Medium/Large حسب الحاجة

### 4.3 توصيات تقنية لتحسين أداء قاعدة البيانات
1. **استخدام PgBouncer (Connection Pooling)**:
   - استخدم سلسلة الاتصال على البورت 6543 بدلاً من 5432
   - يسمح بإدارة آلاف الاتصال الواردة باستخدام عدد صغير من الاتصالات الحقيقية مع قاعدة البيانات

2. **إضافة فهارس (Indexes)**:
   - إضافة فهارس للحقول التي يتم البحث عنها بشكل متكرر (مثل `location` في جدول `profiles`, `booking_id` في جداول مرتبطة بالحجوزات)

3. **استخدام التخزين المؤقت (Caching)**:
   - استخدم خدمة مثل Redis لتخزين نتائج الاستعلامات المتكررة (مثل عرض الجليسات المتاحة)

4. **تحسين استعلامات قاعدة البيانات**:
   - تجنب `SELECT *` واستخدم فقط الأعمدة المطلوبة
   - استخدام عمليات التجميع (Aggregation) في قاعدة البيانات بدلاً من المعالجة في الواجهة الأمامية

---

## 5. قائمة مراجعة شاملة للمتطلبات لواجهة الدفع الإلكترونية

### 5.1 المتطلبات القانونية والامتثال (Compliance)
| البند | الحالة | ملاحظات |
|------|--------|---------|
| الامتثال لـ **PCI DSS v4.0** | ❌ غير منفذ | يُصبح إلزاميًا بعد 31 مارس 2025 |
| الامتثال للقواعد المحلية (مثل قواعد البنك المركزي المصري) | ❌ غير منفذ | يتطلب مراجعة قانونية |
| سياسة خصوصية واضحة لمعالجة البيانات | ❌ غير منفذ | يحتاج إلى إنشاء صفحة سياسة خصوصية |

### 5.2 المتطلبات الأمنية
| البند | الحالة | ملاحظات |
|------|--------|---------|
| تشفير البيانات أثناء النقل (HTTPS/TLS 1.2+) | ❌ غير منفذ | يتطلب تكوين نطاق مخصص مع شهادة SSL |
| عدم تخزين بيانات البطاقات (PAN/CVV) على خوادمنا | ❌ غير منفذ | يجب استخدام بوابة دفع تتحمل مسؤولية ذلك (تكنولوجيا Tokenization) |
| إخفاء رقم البطاقة عند العرض (Masking) | ❌ غير منفذ | مثال: `**** **** **** 1234` |
| استخدام MFA (التحقق بخطوتين) لحسابات الإدارة | ❌ غير منفذ | يُوصى به لزيادة الأمان |
| تسجيل ومراقبة جميع عمليات الدفع (Audit Logs) | ❌ غير منفذ | يحتاج إلى جدول `payments` مفصل |
| فحص الثغرات الأمنية بانتظام | ❌ غير منفذ | يُوصى به كل 3 أشهر |
| تدريب الموظفين على الأمان المالي | ❌ غير منفذ | يُوصى به |

### 5.3 التكامل التقني
| البند | الحالة | ملاحظات |
|------|--------|---------|
| اختيار بوابة دفع مناسبة (مثل PayMob/Fawry/Stripe) | ❌ غير منفذ | يُوصى ببوابة دفع مصريّة للتعامل مع العملاء المصريين |
| إنشاء حساب تجاري في البوابة المختارة | ❌ غير منفذ | يتطلب وثائق الشركة |
| تنفيذ Webhooks لاستلام حالات الدفع من البوابة | ❌ غير منفذ | يستخدم لتحديث حالة الحجز تلقائيًا بعد إتمام الدفع |
| إضافة خاصية إعادة محاولة الدفع الفاشل | ❌ غير منفذ | لتحسين تجربة المستخدم |
| إضافة صفحة حالة الدفع (Success/Cancel) | ❌ غير منفذ | لتنبيه المستخدم بنتيجة الدفع |
| إضافة جدول `payments` في قاعدة البيانات | ✅ مكتمل | تم إضافته إلى `schema.sql` مع جميع الأعمدة والسياسات الأمنية |

### 5.4 تجربة المستخدم
| البند | الحالة | ملاحظات |
|------|--------|---------|
| عرض طرق دفع واضحة (بطاقة/فوري/إنستابي/فودافون كاش) | ✅ موجودة | في `PaymentPage.tsx` |
| رسائل خطأ واضحة في حال فشل الدفع | ❌ غير منفذ | يحتاج إلى تحسين |
| إرسال إشعار تأكيد بعد إتمام الدفع | ❌ غير منفذ | بريد/رسالة نصية/إشعار داخل التطبيق |
| سجل المعاملات للمستخدم | ❌ غير منفذ | ليتمكن المستخدم من رؤية جميع عملياته المالية |

---

## 6. الخطوات القادمة الموصى بها
1. **أولاً**: تشغيل ملف `schema.sql` في محرر SQL في Supabase لإنشاء/تحديث جميع الجداول والسياسات
2. **ثانياً**: تقييم حجم المستخدمين المتوقع واختيار خطة Supabase المناسبة
3. **ثالثاً**: اختيار بوابة دفع ومتابعة إجراءات التسجيل التجاري
4. **رابعاً**: تنفيذ تكامل البوابة مع الواجهات الأمامية والخلفية
5. **خامساً**: إضافة خدمات TypeScript لجداول النزاعات والدعم الفني والدفع ( في `src/services/` )

---

تم إعداد هذا التقرير بتاريخ **2026-06-24**.
